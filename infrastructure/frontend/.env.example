# 같은 도메인의 /api를 사용하므로 기본값은 빈 문자열입니다.
NEXT_PUBLIC_API_BASE_URL=

# One Google Web Client ID is reused by the backend and frontend build.
GOOGLE_OAUTH_CLIENT_ID=example.apps.googleusercontent.com

# 배포 스크립트가 Git SHA 태그를 자동으로 지정합니다.
# 수동 롤백 때만 이전 이미지 태그를 지정하세요.
FRONTEND_IMAGE=zani/frontend:dev
