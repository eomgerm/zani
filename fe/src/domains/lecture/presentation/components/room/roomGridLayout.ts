/**
 * 참가자 타일 배치 설정. 갤러리와 화면 공유 로스터가 같은 값을 쓴다.
 *
 * <p>두 곳이 각자 값을 들고 있으면 한쪽만 바뀌어 같은 참가자가 화면에 따라 다른 비율·간격으로
 * 보인다. 배치기에 넘기는 값은 여기서만 정한다.
 */

/**
 * 타일 가로:세로 비율. 웹캠 스트림 기본 비율(16:9)과 맞춰 영상 위아래가 잘리지 않게 한다(티켓 246).
 * 자르지 않으므로 칸이 이 비율이 아닐 때 남는 자리가 생기는데, 배치기가 그 남는 자리를 최소로 만든다.
 */
export const TILE_ASPECT_RATIO = "16:9";

/** 타일 사이 간격(px). 배치 계산에 들어가는 값이라 CSS gap 이 아니라 숫자로 넘긴다. */
export const TILE_GAP = 12;

/**
 * 한 페이지에 보여주는 최대 타일 수.
 *
 * <p>갤러리·PiP 창·공유 중 옆줄이 같은 값을 쓴다. 화면마다 다르면 같은 인원인데 어디서는 다 보이고
 * 어디서는 잘려, 사용자가 몇 명이 있는지 화면을 옮길 때마다 다시 세게 된다.
 */
export const ITEMS_PER_PAGE = 8;

/**
 * PiP 창에 한 번에 보여줄 참가자 수.
 *
 * <p>PiP 창은 260px 남짓이라 페이지를 나눠도 넘길 버튼을 누를 자리가 마땅치 않고, 공유 화면을
 * 곁눈질하는 창이라 참가자를 다 볼 필요도 없다. 페이지 대신 상한만 두고 나머지는 수로 알린다.
 */
export const PIP_MAX_TILES = 4;

/**
 * 타일 크기 변화는 즉시 반영하고, 자리 이동만 애니메이션한다.
 *
 * <p>배치기는 크기(`width`/`height`)와 자리(`x`/`y`)를 각각 다른 스프링으로 움직인다. 크기 쪽까지
 * 스프링으로 두면 사이드 패널을 여닫을 때마다 문제가 된다 — 패널이 스테이지 폭을 1252→898 로 줄여
 * 배치가 4×3 에서 3×4 로 재구성되는데, `smooth`(감쇠비 ≈0.92)는 부족감쇠라 목표 크기를 지나쳤다
 * 되돌아온다. 참가자가 들고 나는 것도 아닌데 화면 전체가 출렁인다.
 *
 * <p>자리 이동은 그대로 둔다. 새 참가자가 들어와 타일이 밀려나는 것은 실제로 일어난 일이라
 * 따라가는 편이 읽기 쉽다.
 */
export const SNAP_SIZE = { duration: 0 } as const;

/** 타일은 배치기가 정해 준 칸을 그대로 채운다. 크기 계산은 배치기가 이미 끝냈다. */
export const TILE_FILL: React.CSSProperties = { width: "100%", height: "100%" };
